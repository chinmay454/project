

def main():
    print("="*40)
    print("         WELCOME TO THE QUIZ        ")
    print("="*40)
    name = input("Enter your name: ")
    while True:
        quiz = QuizManager(name)
        quiz.start_quiz()
        quiz.show_score()
        quiz.save_score()
        choice = input("\nDo you want to retry? (y/n): ").lower()
        if choice != 'y':
            break
    view = input("View leaderboard? (y/n): ").lower()
    if view == 'y':
        quiz.view_leaderboard()
    print("\nThank you for playing!")

if _name_ == "_main_":
    main()
    all the best
